# ── Web framework ────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── Settings / validation ─────────────────────────────────────────────────────
pydantic
pydantic-settings
python-dotenv

# ── Git integration ───────────────────────────────────────────────────────────
gitpython

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb

# ── Embeddings (CPU-only torch installed first in Dockerfile) ─────────────────
sentence-transformers

# ── LLM HTTP calls (OpenRouter) ───────────────────────────────────────────────
requests
